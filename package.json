{
  "name": "fitai-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend"
  }
}
